<!DOCTYPE html>
{{>header}}
<body>
       {{>navbar}}   
<div class="main-content">

<div class="container">
    <div class="row">
    <div class="col s12 m8 offset-m2">
      
        <div class="card card-panel blue-grey darken-3">
        <div class="card-content white-text">
            <h3 class="center">{{title}}</h3>
            {{!-- <p>Account is unverified, pls confirm your email by clicking on the email link sent to you .</p> --}}
            <div class="center">
                <p>{{description}}</p>   
                <p>{{name}}</p>
                <p>{{email}}</p>
            </div>
            
            </br>
            <form action="/dashboard" method="post">
                <button class="exit btn-large btn-center waves-effect waves-light" type="submit" name="action">Home
                    <i class="material-icons right">exit_to_app</i>
                </button>   
            </form>  
        </div>
        </div>
    </div>
    </div>
</div>

{{>footer}}
